/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => AstraeaPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian2 = require("obsidian");

// src/settings.ts
var DEFAULT_SETTINGS = {
  // Plugin control
  pluginEnabled: true,
  // App styling
  generalFontSize: "",
  generalFontColor: "",
  generalFont: "",
  backgroundPrimary: "",
  backgroundSecondary: "",
  borderColor: "",
  highlightColor: "",
  indentText: false,
  // Editor & Reader - Headers & Titles
  headerTitleColor: "",
  headerTitleFont: "",
  headerFontSize: "",
  enableHeaderSizing: false,
  headerTitleSize: "1",
  headerFontWeight: "bold",
  // Links
  internalLinkColor: "",
  externalLinkColor: "",
  linkUnderline: true,
  // Back-tick formatting
  codeBlockBgColor: "",
  backtickFont: "",
  backtickBg: "",
  // Code block cosmetics
  showCodeBlockBadge: false,
  // Typography
  lineHeight: ""
};

// src/SettingsTab.ts
var import_obsidian = require("obsidian");
var AstraeaSettingTab = class extends import_obsidian.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    containerEl.createEl("h2", { text: "Astraea Theme Settings" });
    new import_obsidian.Setting(containerEl).setName("Enable plugin").setDesc("Toggle the entire plugin on/off").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.pluginEnabled).onChange(async (value) => {
        this.plugin.settings.pluginEnabled = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Manage settings").setDesc("Export, import, or reset your theme configuration").addButton(
      (button) => button.setButtonText("Export").onClick(() => this.exportSettings())
    ).addButton(
      (button) => button.setButtonText("Import").onClick(() => this.importSettings())
    ).addButton(
      (button) => button.setButtonText("Reset to defaults").setWarning().onClick(() => this.resetSettings())
    );
    containerEl.createEl("h3", { text: "App Styling" });
    new import_obsidian.Setting(containerEl).setName("General font size").setDesc("Base font size for all text (e.g., 16px, 1.2em, leave empty for default)").addText(
      (text) => text.setPlaceholder("e.g., 16px").setValue(this.plugin.settings.generalFontSize).onChange(async (value) => {
        this.plugin.settings.generalFontSize = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("General font color").setDesc("Default text color for all content").addColorPicker(
      (color) => color.setValue(this.plugin.settings.generalFontColor || "#000000").onChange(async (value) => {
        this.plugin.settings.generalFontColor = value;
        await this.plugin.saveSettings();
      })
    ).addExtraButton(
      (button) => button.setIcon("reset").setTooltip("Clear color").onClick(async () => {
        this.plugin.settings.generalFontColor = "";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    new import_obsidian.Setting(containerEl).setName("General font").setDesc("Font family for the entire app (leave empty for default)").addText(
      (text) => text.setPlaceholder("e.g., Inter, Roboto, Arial").setValue(this.plugin.settings.generalFont).onChange(async (value) => {
        this.plugin.settings.generalFont = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Background primary").setDesc("Main background color").addColorPicker(
      (color) => color.setValue(this.plugin.settings.backgroundPrimary || "#ffffff").onChange(async (value) => {
        this.plugin.settings.backgroundPrimary = value;
        await this.plugin.saveSettings();
      })
    ).addExtraButton(
      (button) => button.setIcon("reset").setTooltip("Clear color").onClick(async () => {
        this.plugin.settings.backgroundPrimary = "";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Background secondary").setDesc("Secondary background color (sidebars, etc.)").addColorPicker(
      (color) => color.setValue(this.plugin.settings.backgroundSecondary || "#f5f5f5").onChange(async (value) => {
        this.plugin.settings.backgroundSecondary = value;
        await this.plugin.saveSettings();
      })
    ).addExtraButton(
      (button) => button.setIcon("reset").setTooltip("Clear color").onClick(async () => {
        this.plugin.settings.backgroundSecondary = "";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Border color").setDesc("Color for borders and dividers").addColorPicker(
      (color) => color.setValue(this.plugin.settings.borderColor || "#e0e0e0").onChange(async (value) => {
        this.plugin.settings.borderColor = value;
        await this.plugin.saveSettings();
      })
    ).addExtraButton(
      (button) => button.setIcon("reset").setTooltip("Clear color").onClick(async () => {
        this.plugin.settings.borderColor = "";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Highlight color").setDesc("Text selection highlight color").addColorPicker(
      (color) => color.setValue(this.plugin.settings.highlightColor || "#b3d4fc").onChange(async (value) => {
        this.plugin.settings.highlightColor = value;
        await this.plugin.saveSettings();
      })
    ).addExtraButton(
      (button) => button.setIcon("reset").setTooltip("Clear color").onClick(async () => {
        this.plugin.settings.highlightColor = "";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Indent paragraphs").setDesc("Add single-space indent to regular text (excludes headers and code blocks)").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.indentText).onChange(async (value) => {
        this.plugin.settings.indentText = value;
        await this.plugin.saveSettings();
      })
    );
    containerEl.createEl("h3", { text: "Headers & Titles" });
    new import_obsidian.Setting(containerEl).setName("Header & title color").setDesc("Color for all headers (h1-h6) and file title").addColorPicker(
      (color) => color.setValue(this.plugin.settings.headerTitleColor || "#000000").onChange(async (value) => {
        this.plugin.settings.headerTitleColor = value;
        await this.plugin.saveSettings();
      })
    ).addExtraButton(
      (button) => button.setIcon("reset").setTooltip("Clear color").onClick(async () => {
        this.plugin.settings.headerTitleColor = "";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Header & title font").setDesc("Font family for headers and file title (leave empty for default)").addText(
      (text) => text.setPlaceholder("e.g., Georgia, Times New Roman").setValue(this.plugin.settings.headerTitleFont).onChange(async (value) => {
        this.plugin.settings.headerTitleFont = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Header font size").setDesc("Font size for all headers when sizing is disabled (e.g., 18px, 1.2em, leave empty for body text size)").addText(
      (text) => text.setPlaceholder("e.g., 18px").setValue(this.plugin.settings.headerFontSize).onChange(async (value) => {
        this.plugin.settings.headerFontSize = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Enable header sizing").setDesc("Make headers progressively larger (h1 biggest, h6 smallest). When off, all headers use the header font size above.").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.enableHeaderSizing).onChange(async (value) => {
        this.plugin.settings.enableHeaderSizing = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Header size multiplier").setDesc("Adjust header sizes when sizing is enabled (e.g., 1.1 for 110%, leave empty for default)").addText(
      (text) => text.setPlaceholder("e.g., 1.1").setValue(this.plugin.settings.headerTitleSize).onChange(async (value) => {
        this.plugin.settings.headerTitleSize = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Header font weight").setDesc("Font weight for headers").addDropdown(
      (dropdown) => dropdown.addOption("normal", "Normal").addOption("bold", "Bold").addOption("100", "100 (Thin)").addOption("200", "200").addOption("300", "300 (Light)").addOption("400", "400 (Normal)").addOption("500", "500 (Medium)").addOption("600", "600 (Semi-bold)").addOption("700", "700 (Bold)").addOption("800", "800").addOption("900", "900 (Black)").setValue(this.plugin.settings.headerFontWeight).onChange(async (value) => {
        this.plugin.settings.headerFontWeight = value;
        await this.plugin.saveSettings();
      })
    );
    containerEl.createEl("h3", { text: "Links" });
    new import_obsidian.Setting(containerEl).setName("Internal link color").setDesc("Color for internal wiki links").addColorPicker(
      (color) => color.setValue(this.plugin.settings.internalLinkColor || "#7c3aed").onChange(async (value) => {
        this.plugin.settings.internalLinkColor = value;
        await this.plugin.saveSettings();
      })
    ).addExtraButton(
      (button) => button.setIcon("reset").setTooltip("Clear color").onClick(async () => {
        this.plugin.settings.internalLinkColor = "";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    new import_obsidian.Setting(containerEl).setName("External link color").setDesc("Color for external hyperlinks").addColorPicker(
      (color) => color.setValue(this.plugin.settings.externalLinkColor || "#2563eb").onChange(async (value) => {
        this.plugin.settings.externalLinkColor = value;
        await this.plugin.saveSettings();
      })
    ).addExtraButton(
      (button) => button.setIcon("reset").setTooltip("Clear color").onClick(async () => {
        this.plugin.settings.externalLinkColor = "";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Link underline").setDesc("Show underline on links").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.linkUnderline).onChange(async (value) => {
        this.plugin.settings.linkUnderline = value;
        await this.plugin.saveSettings();
      })
    );
    containerEl.createEl("h3", { text: "Code Formatting" });
    new import_obsidian.Setting(containerEl).setName("Code block background color").setDesc("Background color for fenced code blocks").addColorPicker(
      (color) => color.setValue(this.plugin.settings.codeBlockBgColor || "#f5f5f5").onChange(async (value) => {
        this.plugin.settings.codeBlockBgColor = value;
        await this.plugin.saveSettings();
      })
    ).addExtraButton(
      (button) => button.setIcon("reset").setTooltip("Clear color").onClick(async () => {
        this.plugin.settings.codeBlockBgColor = "";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Inline backtick text color").setDesc("Color for inline `code` text").addColorPicker(
      (color) => color.setValue(this.plugin.settings.backtickFont || "#e11d48").onChange(async (value) => {
        this.plugin.settings.backtickFont = value;
        await this.plugin.saveSettings();
      })
    ).addExtraButton(
      (button) => button.setIcon("reset").setTooltip("Clear color").onClick(async () => {
        this.plugin.settings.backtickFont = "";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Inline backtick background").setDesc("Background color for inline `code`").addColorPicker(
      (color) => color.setValue(this.plugin.settings.backtickBg || "#fee2e2").onChange(async (value) => {
        this.plugin.settings.backtickBg = value;
        await this.plugin.saveSettings();
      })
    ).addExtraButton(
      (button) => button.setIcon("reset").setTooltip("Clear color").onClick(async () => {
        this.plugin.settings.backtickBg = "";
        await this.plugin.saveSettings();
        this.display();
      })
    );
    containerEl.createEl("h3", { text: "Code Block Cosmetics" });
    new import_obsidian.Setting(containerEl).setName("Show language badge").setDesc("Display language badge in code blocks (Reading View, Source Mode & Live Preview)").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.showCodeBlockBadge).onChange(async (value) => {
        this.plugin.settings.showCodeBlockBadge = value;
        await this.plugin.saveSettings();
      })
    );
    containerEl.createEl("h3", { text: "Typography" });
    new import_obsidian.Setting(containerEl).setName("Line height").setDesc("Line height for text (e.g., 1.6, leave empty for default)").addText(
      (text) => text.setPlaceholder("e.g., 1.6").setValue(this.plugin.settings.lineHeight).onChange(async (value) => {
        this.plugin.settings.lineHeight = value;
        await this.plugin.saveSettings();
      })
    );
  }
  exportSettings() {
    const data = JSON.stringify(this.plugin.settings, null, 2);
    const blob = new Blob([data], { type: "application/json" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = "astraea-settings.json";
    a.click();
    URL.revokeObjectURL(url);
  }
  async importSettings() {
    const input = document.createElement("input");
    input.type = "file";
    input.accept = "application/json";
    input.onchange = async (e) => {
      var _a;
      const file = (_a = e.target.files) == null ? void 0 : _a[0];
      if (!file) return;
      const text = await file.text();
      try {
        const imported = JSON.parse(text);
        this.plugin.settings = Object.assign({}, DEFAULT_SETTINGS, imported);
        await this.plugin.saveSettings();
        this.display();
      } catch (err) {
        console.error("Failed to import settings:", err);
      }
    };
    input.click();
  }
  resetSettings() {
    new ConfirmResetModal(this.app, async () => {
      this.plugin.settings = Object.assign({}, DEFAULT_SETTINGS);
      await this.plugin.saveSettings();
      this.display();
    }).open();
  }
};
var ConfirmResetModal = class extends import_obsidian.Modal {
  constructor(app, onConfirm) {
    super(app);
    this.onConfirm = onConfirm;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.createEl("h2", { text: "Reset to defaults?" });
    contentEl.createEl("p", {
      text: "This will reset all Astraea settings to their default values. This action cannot be undone."
    });
    const buttonContainer = contentEl.createDiv({ cls: "modal-button-container" });
    buttonContainer.createEl("button", { text: "Cancel" }).addEventListener("click", () => this.close());
    const confirmBtn = buttonContainer.createEl("button", {
      text: "Reset",
      cls: "mod-warning"
    });
    confirmBtn.addEventListener("click", () => {
      this.onConfirm();
      this.close();
    });
  }
  onClose() {
    const { contentEl } = this;
    contentEl.empty();
  }
};

// src/features/_css.ts
function buildCSS(settings) {
  const css = [];
  if (settings.generalFontSize) {
    css.push(`
.markdown-preview-view,
.markdown-source-view,
.markdown-reading-view,
.cm-content,
body {
    font-size: ${settings.generalFontSize} !important;
}
        `);
  }
  if (settings.generalFontColor) {
    css.push(`
/* General text color for all views */
.markdown-preview-view,
.markdown-source-view .cm-line,
.markdown-reading-view,
.cm-content,
body {
    color: ${settings.generalFontColor} !important;
}
        `);
  }
  if (settings.generalFont) {
    css.push(`
body {
    --font-text: ${settings.generalFont} !important;
    --font-interface: ${settings.generalFont} !important;
}
        `);
  }
  if (settings.backgroundPrimary) {
    css.push(`
.theme-light, .theme-dark {
    --background-primary: ${settings.backgroundPrimary} !important;
}
        `);
  }
  if (settings.backgroundSecondary) {
    css.push(`
.theme-light, .theme-dark {
    --background-secondary: ${settings.backgroundSecondary} !important;
}
        `);
  }
  if (settings.borderColor) {
    css.push(`
.theme-light, .theme-dark {
    --background-modifier-border: ${settings.borderColor} !important;
}
        `);
  }
  if (settings.highlightColor) {
    css.push(`
::selection {
    background: ${settings.highlightColor} !important;
}
        `);
  }
  if (settings.indentText) {
    css.push(`
/* Indent regular paragraphs, exclude headers and code blocks */
.markdown-source-view.mod-cm6 .cm-line:not(.HyperMD-header):not(.HyperMD-codeblock) {
    padding-left: 1ch;
}

.markdown-preview-view p:not(.HyperMD-header) {
    padding-left: 1ch;
}

.markdown-reading-view p {
    padding-left: 1ch;
}
        `);
  }
  if (settings.headerTitleColor) {
    css.push(`
/* Reading View */
.markdown-preview-view h1, .markdown-preview-view h2, .markdown-preview-view h3,
.markdown-preview-view h4, .markdown-preview-view h5, .markdown-preview-view h6,
/* Source Mode */
.markdown-source-view .cm-header,
/* Live Preview */
.cm-s-obsidian .HyperMD-header,
.cm-line.HyperMD-header-1, .cm-line.HyperMD-header-2, .cm-line.HyperMD-header-3,
.cm-line.HyperMD-header-4, .cm-line.HyperMD-header-5, .cm-line.HyperMD-header-6,
/* File Title */
.inline-title {
    color: ${settings.headerTitleColor} !important;
}
        `);
  }
  if (settings.headerTitleFont) {
    css.push(`
/* Reading View */
.markdown-preview-view h1, .markdown-preview-view h2, .markdown-preview-view h3,
.markdown-preview-view h4, .markdown-preview-view h5, .markdown-preview-view h6,
/* Source Mode */
.markdown-source-view .cm-header,
/* Live Preview */
.cm-s-obsidian .HyperMD-header,
.cm-line.HyperMD-header-1, .cm-line.HyperMD-header-2, .cm-line.HyperMD-header-3,
.cm-line.HyperMD-header-4, .cm-line.HyperMD-header-5, .cm-line.HyperMD-header-6,
/* File Title */
.inline-title {
    font-family: ${settings.headerTitleFont} !important;
}
        `);
  }
  if (!settings.enableHeaderSizing) {
    const headerSize = settings.headerFontSize || "1em";
    css.push(`
/* Reading View */
.markdown-preview-view h1, .markdown-preview-view h2, .markdown-preview-view h3,
.markdown-preview-view h4, .markdown-preview-view h5, .markdown-preview-view h6 {
    font-size: ${headerSize} !important;
    line-height: inherit !important;
    margin-top: 0 !important;
    margin-bottom: 0 !important;
}

/* Source Mode */
.markdown-source-view .cm-header-1,
.markdown-source-view .cm-header-2,
.markdown-source-view .cm-header-3,
.markdown-source-view .cm-header-4,
.markdown-source-view .cm-header-5,
.markdown-source-view .cm-header-6 {
    font-size: ${headerSize} !important;
    line-height: inherit !important;
}

/* Live Preview - THIS WAS MISSING */
.cm-s-obsidian .HyperMD-header-1,
.cm-s-obsidian .HyperMD-header-2,
.cm-s-obsidian .HyperMD-header-3,
.cm-s-obsidian .HyperMD-header-4,
.cm-s-obsidian .HyperMD-header-5,
.cm-s-obsidian .HyperMD-header-6 {
    font-size: ${headerSize} !important;
    line-height: inherit !important;
}

/* CM6 Editor (Live Preview) */
.cm-line.HyperMD-header-1,
.cm-line.HyperMD-header-2,
.cm-line.HyperMD-header-3,
.cm-line.HyperMD-header-4,
.cm-line.HyperMD-header-5,
.cm-line.HyperMD-header-6 {
    font-size: ${headerSize} !important;
    line-height: inherit !important;
}

/* File title */
.inline-title {
    font-size: ${headerSize} !important;
}
        `);
  }
  if (settings.enableHeaderSizing && settings.headerTitleSize) {
    css.push(`
/* Reading View */
.markdown-preview-view h1 { font-size: calc(2em * ${settings.headerTitleSize}) !important; }
.markdown-preview-view h2 { font-size: calc(1.5em * ${settings.headerTitleSize}) !important; }
.markdown-preview-view h3 { font-size: calc(1.25em * ${settings.headerTitleSize}) !important; }
.markdown-preview-view h4 { font-size: calc(1.1em * ${settings.headerTitleSize}) !important; }
.markdown-preview-view h5 { font-size: calc(1em * ${settings.headerTitleSize}) !important; }
.markdown-preview-view h6 { font-size: calc(0.9em * ${settings.headerTitleSize}) !important; }

/* Source Mode */
.markdown-source-view .cm-header-1 { font-size: calc(2em * ${settings.headerTitleSize}) !important; }
.markdown-source-view .cm-header-2 { font-size: calc(1.5em * ${settings.headerTitleSize}) !important; }
.markdown-source-view .cm-header-3 { font-size: calc(1.25em * ${settings.headerTitleSize}) !important; }
.markdown-source-view .cm-header-4 { font-size: calc(1.1em * ${settings.headerTitleSize}) !important; }
.markdown-source-view .cm-header-5 { font-size: calc(1em * ${settings.headerTitleSize}) !important; }
.markdown-source-view .cm-header-6 { font-size: calc(0.9em * ${settings.headerTitleSize}) !important; }

/* Live Preview - CM6 */
.cm-line.HyperMD-header-1 { font-size: calc(2em * ${settings.headerTitleSize}) !important; }
.cm-line.HyperMD-header-2 { font-size: calc(1.5em * ${settings.headerTitleSize}) !important; }
.cm-line.HyperMD-header-3 { font-size: calc(1.25em * ${settings.headerTitleSize}) !important; }
.cm-line.HyperMD-header-4 { font-size: calc(1.1em * ${settings.headerTitleSize}) !important; }
.cm-line.HyperMD-header-5 { font-size: calc(1em * ${settings.headerTitleSize}) !important; }
.cm-line.HyperMD-header-6 { font-size: calc(0.9em * ${settings.headerTitleSize}) !important; }

.cm-s-obsidian .HyperMD-header-1 { font-size: calc(2em * ${settings.headerTitleSize}) !important; }
.cm-s-obsidian .HyperMD-header-2 { font-size: calc(1.5em * ${settings.headerTitleSize}) !important; }
.cm-s-obsidian .HyperMD-header-3 { font-size: calc(1.25em * ${settings.headerTitleSize}) !important; }
.cm-s-obsidian .HyperMD-header-4 { font-size: calc(1.1em * ${settings.headerTitleSize}) !important; }
.cm-s-obsidian .HyperMD-header-5 { font-size: calc(1em * ${settings.headerTitleSize}) !important; }
.cm-s-obsidian .HyperMD-header-6 { font-size: calc(0.9em * ${settings.headerTitleSize}) !important; }
        `);
  }
  if (settings.headerFontWeight) {
    css.push(`
/* Reading View */
.markdown-preview-view h1, .markdown-preview-view h2, .markdown-preview-view h3,
.markdown-preview-view h4, .markdown-preview-view h5, .markdown-preview-view h6,
/* Source Mode */
.markdown-source-view .cm-header,
/* Live Preview */
.cm-s-obsidian .HyperMD-header,
.cm-line.HyperMD-header-1, .cm-line.HyperMD-header-2, .cm-line.HyperMD-header-3,
.cm-line.HyperMD-header-4, .cm-line.HyperMD-header-5, .cm-line.HyperMD-header-6,
/* File Title */
.inline-title {
    font-weight: ${settings.headerFontWeight} !important;
}
        `);
  }
  if (settings.internalLinkColor) {
    css.push(`
/* Internal links - all parts */
.markdown-preview-view .internal-link,
.markdown-source-view .cm-hmd-internal-link {
    color: ${settings.internalLinkColor} !important;
}
        `);
  }
  if (settings.externalLinkColor) {
    css.push(`
/* External links - Reading View */
.markdown-preview-view a.external-link {
    color: ${settings.externalLinkColor} !important;
}

/* External links - Source Mode (not Live Preview) */
.markdown-source-view:not(.is-live-preview) .cm-link,
.markdown-source-view:not(.is-live-preview) .cm-url,
.markdown-source-view:not(.is-live-preview) .cm-string.cm-url {
    color: ${settings.externalLinkColor} !important;
}

/* External links - Live Preview - All parts of [text](url) */
.markdown-source-view.is-live-preview .cm-link:not(.cm-hmd-internal-link),
.markdown-source-view.is-live-preview .cm-url:not(.cm-hmd-internal-link),
.markdown-source-view.is-live-preview .cm-string:not(.cm-hmd-internal-link),
.markdown-source-view.is-live-preview .cm-formatting-link:not(.cm-hmd-internal-link),
.markdown-source-view.is-live-preview .cm-formatting-link-string:not(.cm-hmd-internal-link),
/* Live Preview - rendered link */
.markdown-rendered .external-link {
    color: ${settings.externalLinkColor} !important;
}

/* Live Preview CM6 - be very specific */
.cm-editor.cm-s-obsidian .cm-link:not(.cm-hmd-internal-link):not(.cm-formatting-link-start):not(.cm-formatting-link-end),
.cm-editor.cm-s-obsidian .cm-url:not(.cm-hmd-internal-link) {
    color: ${settings.externalLinkColor} !important;
}
        `);
  }
  if (!settings.linkUnderline) {
    css.push(`
.markdown-preview-view a,
.markdown-source-view .cm-url,
.markdown-source-view .cm-hmd-internal-link {
    text-decoration: none !important;
}
        `);
  }
  if (settings.codeBlockBgColor) {
    css.push(`
/* Fenced code blocks background - Reading View */
.markdown-preview-view pre {
    background: ${settings.codeBlockBgColor} !important;
}

/* Fenced code blocks background - Source Mode */
.markdown-source-view .HyperMD-codeblock-bg {
    background: ${settings.codeBlockBgColor} !important;
}

/* Fenced code blocks background - Live Preview */
.cm-s-obsidian pre.HyperMD-codeblock,
.HyperMD-codeblock-begin,
.HyperMD-codeblock-end,
.HyperMD-codeblock {
    background: ${settings.codeBlockBgColor} !important;
}
        `);
  }
  if (settings.backtickFont || settings.backtickBg) {
    css.push(`
/* Inline backticks only - Reading View */
.markdown-preview-view :not(pre) > code {
    ${settings.backtickFont ? `color: ${settings.backtickFont} !important;` : ""}
    ${settings.backtickBg ? `background: ${settings.backtickBg} !important;` : ""}
}

/* Inline backticks only - Source Mode */
.markdown-source-view .cm-inline-code {
    ${settings.backtickFont ? `color: ${settings.backtickFont} !important;` : ""}
    ${settings.backtickBg ? `background: ${settings.backtickBg} !important;` : ""}
}

/* Inline backticks only - Live Preview */
.cm-s-obsidian span.cm-inline-code {
    ${settings.backtickFont ? `color: ${settings.backtickFont} !important;` : ""}
    ${settings.backtickBg ? `background: ${settings.backtickBg} !important;` : ""}
}
        `);
  }
  if (settings.showCodeBlockBadge) {
    css.push(`
/* Language badge - top right */
.astraea-lang-badge {
    position: absolute;
    top: 4px;
    right: 4px;
    padding: 2px 8px;
    font-size: 0.75em;
    font-family: var(--font-monospace);
    background: var(--background-secondary);
    color: var(--text-muted);
    border-radius: 4px;
    pointer-events: none;
    z-index: 1;
}

/* Don't interfere with copy button - let it position dynamically */
.markdown-preview-view pre,
.markdown-source-view .HyperMD-codeblock {
    position: relative;
}
        `);
  }
  css.push(`
/* Horizontal rule (---) spacing normalization */
/* Reading View */
.markdown-preview-view hr {
    margin-top: 1.5em !important;
    margin-bottom: 1.5em !important;
}

/* Source Mode (not Live Preview) */
.markdown-source-view:not(.is-live-preview) hr,
.markdown-source-view:not(.is-live-preview) .cm-line .HyperMD-hr {
    margin-top: 1.5em !important;
    margin-bottom: 1.5em !important;
}

/* Live Preview - the actual HR line */
.markdown-source-view.is-live-preview .cm-line:has(.cm-hr),
.markdown-source-view.is-live-preview .cm-line .cm-hr,
.cm-editor .cm-line:has(hr) {
    margin-top: 1.5em !important;
    margin-bottom: 1.5em !important;
    padding-top: 0 !important;
    padding-bottom: 0 !important;
}

/* Live Preview - HR element itself */
.markdown-source-view.is-live-preview hr,
.cm-editor hr {
    margin-top: 0 !important;
    margin-bottom: 0 !important;
}
    `);
  if (settings.lineHeight) {
    css.push(`
/* Apply line height to all text content */
.markdown-preview-view,
.markdown-preview-view p,
.markdown-source-view .cm-line,
.markdown-reading-view,
body {
    line-height: ${settings.lineHeight} !important;
}
        `);
  }
  return css.join("\n");
}

// src/features/codeBlockBadge.ts
function addLangBadges() {
  document.querySelectorAll(".markdown-preview-view pre").forEach((pre) => {
    if (pre.querySelector(".astraea-lang-badge")) return;
    const codeEl = pre.querySelector("code");
    if (!codeEl) return;
    const langMatch = codeEl.className.match(/language-(\S+)/);
    if (!langMatch) return;
    const lang = langMatch[1];
    const badge = createBadge(lang);
    pre.appendChild(badge);
  });
  document.querySelectorAll(".markdown-source-view .HyperMD-codeblock-begin").forEach((beginEl) => {
    const container = beginEl.closest(".cm-line");
    if (!container) return;
    const codeBlockParent = container.parentElement;
    if (!codeBlockParent || codeBlockParent.querySelector(".astraea-lang-badge")) return;
    const fenceText = beginEl.textContent || "";
    const langMatch = fenceText.match(/^```(\S+)/);
    if (!langMatch) return;
    const lang = langMatch[1];
    const badge = createBadge(lang);
    codeBlockParent.style.position = "relative";
    codeBlockParent.appendChild(badge);
  });
  document.querySelectorAll(".cm-editor .cm-line.HyperMD-codeblock-begin").forEach((beginEl) => {
    let codeBlockContainer = beginEl.parentElement;
    while (codeBlockContainer && !codeBlockContainer.classList.contains("cm-content")) {
      if (codeBlockContainer.querySelector(".astraea-lang-badge")) return;
      const fenceText = beginEl.textContent || "";
      const langMatch = fenceText.match(/^```(\S+)/);
      if (langMatch) {
        const lang = langMatch[1];
        const badge = createBadge(lang);
        codeBlockContainer.style.position = "relative";
        codeBlockContainer.appendChild(badge);
        return;
      }
      codeBlockContainer = codeBlockContainer.parentElement;
    }
  });
}
function removeLangBadges() {
  document.querySelectorAll(".astraea-lang-badge").forEach((badge) => {
    badge.remove();
  });
}
function createBadge(language) {
  const badge = document.createElement("div");
  badge.className = "astraea-lang-badge";
  badge.textContent = language;
  return badge;
}

// src/main.ts
var AstraeaPlugin = class extends import_obsidian2.Plugin {
  constructor() {
    super(...arguments);
    this.styleEl = null;
  }
  async onload() {
    await this.loadSettings();
    this.addSettingTab(new AstraeaSettingTab(this.app, this));
    this.applyStyles();
    if (this.settings.showCodeBlockBadge) {
      this.registerEvent(
        this.app.workspace.on("layout-change", () => addLangBadges())
      );
      addLangBadges();
    }
  }
  onunload() {
    this.removeStyles();
    removeLangBadges();
  }
  async loadSettings() {
    this.settings = Object.assign({}, DEFAULT_SETTINGS, await this.loadData());
  }
  async saveSettings() {
    await this.saveData(this.settings);
    this.applyStyles();
    if (this.settings.showCodeBlockBadge) {
      addLangBadges();
    } else {
      removeLangBadges();
    }
  }
  applyStyles() {
    if (!this.settings.pluginEnabled) {
      this.removeStyles();
      return;
    }
    this.removeStyles();
    this.styleEl = document.createElement("style");
    this.styleEl.id = "astraea-custom-styles";
    this.styleEl.textContent = buildCSS(this.settings);
    document.head.appendChild(this.styleEl);
  }
  removeStyles() {
    if (this.styleEl) {
      this.styleEl.remove();
      this.styleEl = null;
    }
  }
};
